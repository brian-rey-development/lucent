export const CATALOG_HELP = `usage: lucent catalog [<verb> [--schema]]
       lucent catalog --codes

Lists every verb, one line each. With a verb, prints that verb's line, or its JSON Schema with --schema.

  --schema   the verb's JSON Schema
  --codes    every diagnostic code`;
